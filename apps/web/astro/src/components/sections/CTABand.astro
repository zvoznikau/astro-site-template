---
import { brand } from '../../config/brand'
---

<!-- CTA Band: Astro.build style -->
<section class="relative max-w-screen-xl mx-auto px-4 sm:px-6 lg:px-8 py-24 sm:py-32 overflow-hidden">
  <!-- Background gradient -->
  <div class="absolute inset-0 bg-gradient-to-br from-primary/10 via-transparent to-accent/10 pointer-events-none"></div>
  <div class="relative z-10">
  <div class="max-w-3xl mx-auto text-center space-y-8">
    <h2 class="text-3xl sm:text-4xl lg:text-5xl font-bold text-base-content">
      Start monitoring your servers today
    </h2>
    <p class="text-xl text-base-content/80">
      Catch issues before they affect your users.
    </p>
    <div class="pt-4">
      <a
        href={brand.urls.appStore}
        class="inline-flex items-center justify-center px-10 py-5 text-lg font-bold text-white rounded-lg bg-blue-600 hover:bg-blue-500 transition-all shadow-lg hover:shadow-blue-500/40 hover:scale-[1.02]"
        target="_blank"
        rel="noopener noreferrer"
      >
        {brand.cta.primary}
        <svg class="ml-2 w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2.5">
          <path stroke-linecap="round" stroke-linejoin="round" d="M13 7l5 5m0 0l-5 5m5-5H6" />
        </svg>
      </a>
    </div>
  </div>
  </div>
</section>
